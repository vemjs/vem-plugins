{
  "name": "@vemjs/plugins-root",
  "private": true,
  "scripts": {
    "build": "for dir in autopairs git layout-customizer lualine telescope treesitter trim-whitespace; do (cd $dir && bun run build); done",
    "test": "bun test",
    "format": "prettier --write .",
    "lint": "oxlint ."
  },
  "devDependencies": {
    "@vemjs/core": "^0.1.3",
    "@vemjs/plugin-api": "^0.1.3",
    "oxlint": "^1.72.0",
    "prettier": "^3.9.4",
    "typescript": "~6.0.2"
  }
}
